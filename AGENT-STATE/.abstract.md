# ScamCity Agent State / L0

- 更新时间：2026-09-21T02:03:06+08:00
- 更新者：root
- 分支 HEAD：无可用 Git 工作树（已核对）
- 当前部署产物：`D:\Minecraft\mods\scamcity-bridge-0.1.0.jar`（同步于 `minecraft-staging/mods/` 与 `build/libs/`），SHA-256 `a3d289a62a27970fc46b3a9d3e805b445636dc55859de3f768004895434313f4`
- 状态：ScamCity 已具备 Web LIVE API、结构化观众事件、Minecraft bridge 干预回写、现场报告和本地跨 Agent 协调层；本轮新增从当前快照追加比较、带预算与过期保护的单轮干预 Agent、市民故事焦点、叙述结果一致性约束，并通过 TypeScript、HTTP 契约、专项脚本和 Edge 浏览器验收。Minecraft JAR 未因本轮 Web 改动重建，仍为 `a3d289a6...`；真实游戏内新链路仍待验收。

本轮（2026-09-21）Web 改进已完成：`compare` 支持 `mode: "snapshot"` 并返回比较来源上下文；`POST /api/agent/intervene` 在激活预算内观察、选择并执行一个白名单干预，模型不可用时走规则/回退，快照变化则 409；叙述模型只能解释引擎已记录的决策，不得改写 blocked/cancelled 等结果。项目仍非 Git 工作树。

**首次真机加载已发生**（2026-09-19 17:16:56）：日志确认 `ScamCity bridge loaded`，模组在 Fabric 1.21.11 / Loader 0.19.3 下可正常加载。历史上的两个长期阻塞项**已全部解除**：（1）HttpClient 默认 HTTP/2 取不到快照（已修为 `HTTP_1_1`）；（2）存档未开作弊导致 `summon`/`kill` 被拒——2026-09-20 08:20 的会话日志全程无 `Unknown or incomplete command` 且 status 显示「已发送 450 条」，证明用户已开 `Allow Cheats`（**会话级设置，每次进世界需重做**）。

本轮（2026-09-20 第三段，用户要求参考 `harrythentrepreneur/omo-minecraft` 取长补短）落地两项，都对准已记录的真实阻塞：

1. **屏幕叠层**（借 omo 的 BossBar/HUD 思路）：新增 `HudState.java` + `ScamCityHud.java`，经 `HudElementRegistry.addLast` 注册，纯 `fill`/`drawText` 无贴图。承载数据源、居民/骗子/受害数、损失与 **comparison verdict**，因此关闭了 A-19 的「Minecraft 端不显示主判定」缺口。选叠层的理由是它**不占世界实体预算**，而预算已被 100 市民+标签占满。已用 `javap` 核对本项目现用的 `fabric-api 0.141.6+1.21.11` 确实提供该 API 且签名一致。**游戏内从未看过，帧数未测。**

2. **端口自发现**：新增 `ApiEndpoints.java` + `scripts/scamcity-serve.mjs` + `scripts/lib/endpoint.mjs`。`npm run serve` 把实际绑定 URL 原子写入 `<tmpdir>/scamcity-endpoint.json`，各端自动读取，且**响应必须含 `schemaVersion` 才被接受**——「端口有人应答」不等于「ScamCity 在应答」，这正是 08:40 那次误判的信号。已实测复现原故障（假监听器占 3000 → serve 改用 51930 → doctor/report 正确定位）。顺带修掉 `doctor`/`report`/`bridge-harness` 里同源的硬编码 3000 缺陷（doctor 此前会在服务健康时报 FAIL），并把 verdict 写入 report。

不要踩：profile 必须选 `fabric-loader-1.21.11`（不是 `Agentcraft`）；人形市民/骗子的五条命令拆分不得「优化」回单条（内联装备 NBT 实测 561 字符，超 256 协议上限会被静默丢弃，后果是一个都画不出且不报错）；JAR 不热加载；**新增市民状态时必须同时改 Java switch、图例行和 `verify-command-format.mjs`**（它从 `types/index.ts` 解析 union 后逐态断言），市民 payload 没有 `riskScore` 字段。

本轮（2026-09-20 第四段，用户问「演示版少了点什么」）结论与提问方向相反：**缺的不是工具，是已有资产没接线**（`recharts` 已装从未 import 就是同一症状）。修掉两处真缺口：

1. **LLM 已接通但前端完全没用**：`/api/llm/thoughts` 实测返回 `mode:live, model:deepseek-v4-flash`，市民用自己的话解释决策且与引擎判定一致，但全仓无前端调用点。新增 dashboard「04 / CITIZEN NARRATION」面板，**仅 live 模式提供**（该路由读服务端 store，local 模式会描述另一座城市）。显式标注 `MODEL`/`RULES`、模型名、延迟、逐条 fallback 与「synthetic citizens」。

2. **Minecraft 端状态映射残缺**：`status()` 只认 `victim`/`protected`，`engaged`/`trusted`/`clicked` 全落到兜底绿色——正在被骗的人显示为安全，而「看见骗局如何展开」正是产品主张。已改为七态精确 switch + 八项图例，并删掉按 `riskScore` 判橙色的死代码（该字段在 payload 中从不存在）。顺带把骗子也人形化（紫甲持书），并把一条硬编码计数断言改写为遍历所有 `summon` 模板的不变量。

下一步：进世界看七态配色与图例是否远看可辨、`style people` 后骗子是否为持书紫甲小人、叠层是否可读、`/scamcity api` 是否读到发现文件，然后跑完 RUNBOOK 七项；浏览器里点一次 NARRATE 看排版。接手先读 AGENT-STATE://memories/current-iteration.md。

详情导航：AGENT-STATE://.overview.md
