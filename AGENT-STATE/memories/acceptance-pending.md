# 待验收事项

- **2026-09-21 Web 改进已验收**：当前快照比较、受约束干预 Agent、浏览器市民故事/叙述/比较交互均有真实命令或浏览器证据，详见 `docs/worklogs/2026-09-21-agent-improvements.md`。专项检查不含真实付费模型调用；`source=rules` 的 Agent 路径已真实执行本地 store 工具。
- **仍待验收**：新增 Web 结果尚未接入 Minecraft HUD 的专门展示；本轮未重建或安装 JAR，故不宣称 Minecraft 现场显示这些 Web 改进。原有七态配色、人物模式、HUD 的真机观感待办仍有效。

- **七态市民配色 + 图例（本轮新增，真机未看过）**：`status()` 从「只认 victim/protected」改为按 `types/index.ts` 的 `CitizenState` 七态精确分支：`safe` 绿 / `suspicious` 蓝 / `engaged` 黄 / `trusted` 橙 / `clicked` 粉 / `victim` 红 / `protected` 青，控制塔图例同步扩为八项（含骗子紫）。修前实测该局 100 市民中有 1 个 `engaged` + 2 个 `trusted` 被误标为绿色安全。同轮删除按 `riskScore` 判橙色的死代码（市民 payload 从无此字段）。已用 `verify-command-format.mjs` 从 TS union 解析后逐态断言（含块颜色不重复、不得复用骗子紫、图例必含全部标签），并做过负向测试。**未验收**：游戏内从未看过；`engaged`/`trusted`/`clicked` 三色故意与网页的三档橙色发散（100 格网格远看会糊成一片），**远看可辨性属观感，未测量**。

- **骗子人形化（本轮新增，真机未看过）**：`/scamcity style people` 原先只作用于市民，骗子始终是紫色方块；现新增 `addScammerStand`（紫甲 + 主手 `writable_book`）。手持物件是必要的：市民也人形化后，单靠甲色在远处分不出身份。字段改名 `humanoidCitizens` → `humanoidFigures`，提示语改为「人物样式」。命令长度实测最长 154 字符（远低于 256 协议上限），已加断言按最宽取值渲染后校长。实际骗子只有 5 个（渲染上限 12），故盔甲架总数 100 → 105。**未验收**：游戏内未看过，帧数代价未测；默认仍为色块。

- **叙述面板（本轮新增，浏览器现场未看过）**：dashboard 新增「04 / CITIZEN NARRATION」面板，接 `/api/llm/thoughts`。该路由之前**已接通但全仓无任何前端调用点**（实测 `mode:live, model:deepseek-v4-flash, 7.7s`）。**仅 live 模式提供**：它读服务端 store，local 模式下 dashboard 跑自己的世界，混用会让叙述描述另一座城市。已验证页面真的渲染出面板（curl 到 `CITIZEN NARRATION`/`thoughts-panel`），且 local 模式正确显示说明文案而非按钮。**未验收**：没在浏览器里真正点过那个按钮，故叙述文字的排版、长文本溢出、`source:fallback` 橙色标注的实际观感均未看过；45s 超时是拍的（参考值：2 个市民 7.7s）4 个市民的真实耗时未测。

- **屏幕叠层 HUD（本轮新增，真机未看过）**：新增 `HudState.java` + `ScamCityHud.java`，经 `HudElementRegistry.addLast` 注册，在左上角绘制数据源、居民/骗子/受害数、损失与 **comparison verdict**。选叠层而非新增实体的原因：实体预算已被 100 市民+标签占满，人形模式还要翻倍，而叠层不占世界实体、也走不出画面。已用 `javap` 核对 `fabric-api 0.141.6+1.21.11` 确实提供 hud 包，且 `HudElement.render(DrawContext, RenderTickCounter)`、`HudElementRegistry.addLast`、`DrawContext.fill/drawText` 签名均一致；`HudState` 有回归断言（含「DEMO 绝不显示成 LIVE」「verdict 必带 disclaimer」「不自行编造出处」）。**未验收**：游戏内从未看过，实际绘制位置、可读性、与 F3/聊天的互让、**帧数代价**均未测量（证据：`docs/worklogs/2026-09-20-hud-and-endpoint-discovery.md`）。

- **端口自发现（本轮新增，已实测复现原故障，真机未验）**：`npm run serve` 把实际绑定的 URL 原子写入 `<tmpdir>/scamcity-endpoint.json`，bridge（`ApiEndpoints.java`）与 Node 工具链均自动读取，且**响应必须含 `schemaVersion=scamcity.simulation/v1` 才被接受**——端口有人应答不等于 ScamCity 在应答，这正是 08:40 那次误判的信号。已实测：假监听器占据 3000 时 serve 自动改用 57662/51930，`doctor`/`report` 正确找到真实服务并标明所用端口（修前 doctor 会在服务健康时报 FAIL）；Java 读取侧另用探针对**真实 serve 输出**复算通过。**未验收**：尚未在 Minecraft 里跑 `/scamcity api` 看它是否真的读到发现文件。

- **真实 Minecraft E2E（部分解除，余下待验收）**：当前 JAR 是 `a3d289a6...`（七态配色 + 骗子人形，2026-09-20 16:20 安装，`tasklist` 确认 `javaw` 计数 0，**游戏内未看过**）。历史基线与备份对应关系以 `AGENT-STATE://resources/artifacts.md` 为单一权威源，此处不重复维护。真机加载过的版本共三个：`243d4dfe...`（08:10，仅离线演示数据）、`3938312a...`（12:26）、`0882f447...`（13:01）。**已在 2026-09-20 08:10～08:21 的真机会话中解除两个长期阻塞项**：（1）存档已开 `Allow Cheats`，日志全程无 `Unknown or incomplete command`，`summon` 执行成功；（2）`增量基准 已建立 / 跳过 3 帧` 首次在游戏内出现。**但那次会话跑的是 `243d4dfe...` 且数据源为 `离线演示`**。live API 路径已于 2026-09-20 下午走通（用户 `refresh` 成功，城市已生成）。仍需以 `fabric-loader-1.21.11` profile（**不是 `Agentcraft`**）重启，并在允许命令的世界跑完 RUNBOOK 的现场清单十一项（证据：`docs/worklogs/2026-09-20-narration-and-state-colors.md`）。
- **API 地址（原「必须显式指定，未解决」，本轮已解决，见上方端口自发现条目）**：历史根因保留备查——2026-09-20 的会话中端口 3000 的监听进程是 **Minecraft 自己**（PID 21064 = `javaw.exe`，`Open to LAN` 分到了 3000），bridge 连上自己的 LAN 服务器后报 `HTTP/1.1 header parser received no bytes`。此报错与 2026-09-19 的 HTTP/2 故障**文字相同但根因不同**，不要混淆。现在 bridge 会按序尝试候选端点并校验 `schemaVersion`，因此不再需要手工对齐端口；**仍然不要**把服务改到 3000「对上默认值」，那个端口会被 LAN 反复占用。启动器 GUI 起的游戏不继承 shell 环境变量，所以 `SCAMCITY_API` 对它无效，需要显式指定时用 `-Dscamcity.api=...`。
- **模组可加载（已验证）**：2026-09-19 17:16:56 首次真机启动，`D:\Minecraft\logs\latest.log` 出现 `ScamCity bridge loaded; use /scamcity refresh or /scamcity demo`，并列出 `scamcity_bridge 0.1.0`。命令注册与回显（`api`/`anchor`/`clear`/`status`/`demo`）正常，离线回退路径按设计工作且标注 `DEMO`。注意：那次加载的是 `7adfb509...`。HTTP/1.1 修复其后已由真机验证：`3938312a...`（12:26）与 `0882f447...`（13:01）均加载成功，且下午的 `refresh` 拉到真实快照、城市已生成。
- **HTTP/1.1 修复的真机验证（待验收）**：首次真机运行暂发现 `HttpClient` 默认 HTTP/2 导致取不到快照（`IOException: HTTP/1.1 header parser received no bytes`，请求不达服务端）。已固定为 `HTTP_1_1`，Java 探针连跑 5 次零偏差（HTTP_2 必败、HTTP_1_1 必成 200 / 130191 字节，代理已排除）。但**游戏内尚未验证**：需确认 `status` 显示 `HTTP` 与真实 `snapshot=42-0-2-0`，而非 `offline-demo-*`。
- **市民稳定投影现场确认（待验收）**：`CitizenGrid` 已用 id 稳定哈希取代数组下标，离线回归与真实 API 数据复算均通过（满编 100 人为固定置换）。首次真机日志已可见预期标签（`citizen-059 安全`、`citizen-068 高风险`、`scammer-02 SCAMMER`）且坐标按 cell 递增。仍需现场确认：状态变化时方块不跳位、标签与 Web 端同一 citizen 一致。A-13 退出条件中的 `layout.json` 外部布局文件**未实现**，不得宣称 A-13 已完全关闭。
- **权限状态（已解除，2026-09-20 08:20 确认）**：2026-09-19 首次真机时 `summon`/`kill`/`title` 全部被拒，回显 `Unknown or incomplete command`（连不带 NBT 的 `kill @e[tag=scamcity]` 也被拒，故不是命令格式问题）。根因：存档 `Agent Education` 的 `level.dat` 中 `allowCommands=0`、`GameType=0`（生存），而 bridge 是纯客户端模组，靠玩家身份发命令。**现已解决**：2026-09-20 08:10～08:21 的会话日志全程无 `Unknown or incomplete command`，且 status 显示 `已发送 450 条`，证明 `summon` 执行成功，用户已在 GUI 开启 `Allow Cheats`。注意这是**会话级**设置：每次重新进入单人世界都需重做 `ESC` → `Open to LAN` → `Allow Cheats: ON` → `Start LAN World`，存档本身未被修改。另外：`npm run doctor` 的 `command-errors: no parser errors` 在零命令发出时是**空过**的，不能当作权限已开的证据。长期改进项仍是无副作用权限探针。
- **市民人形化（本轮新增小试版，真机未看过）**：新增 `/scamcity style blocks|people`，`people` 把市民从 `block_display` 色块改为染色皮甲盔甲架（四件套装，颜色沉底与色块模式一致）。**默认仍为色块**：100 个盔甲架比 100 个 display 实体重，帧数代价未测量，便宜模式留作退路。切换时 `scenes.reset(true)` 丢弃增量基准（色块与盔甲架不能互相 diff）。**关键约束（不要“优化”回单条命令）**：内联装备 NBT 的 `summon` 实测 561 字符，而命令经 `sendChatCommand` 发出、协议上限 256（`add()` 的 250 是余量），超限会被**静默丢弃**——后果是 100 个市民一个也画不出且界面不报错。故拆为裸 `summon`（155）+ 四条 `item replace`（各约 116），五条落在同一槽位所以不破坏 A-12。`item replace` 的 `type=armor_stand` 不可省：标签文字 `text_display` 与盔甲架共用槽位标签，裸标签选择器会给文字穿衣。上述两点已由 `verify-command-format.mjs` 断言锁定。代价：单市民 2 → 6 条命令，状态变化增量 3 → 7 条，满编全量约 500 条。**未验收**：100 个盔甲架帧数未测；人形模式在真机未看过；`Rotation:[180f,0f]` 使所有市民同向，观感呆板但改为按 cell 算朝向会给“同 seed 同画面”多一个变量，故未动。
- **性能/增量渲染（本轮改为按槽位增量，离线已验证）**：Fabric bridge 不再每次全量清理重建。新增 `Scene`（`minecraft-bridge/src/main/java/.../Scene.java`）让每个展示实体带共享 `scamcity` 标签 + 自己的槽位标签（`sc_c<cell>` / `sc_s<i>` / `sc_e<i>` / `sc_h<i>`），`SceneQueue` 以**最后一次完整渲染完的帧**为基准算 delta。离线回归断言：满编 100 格城市里单个市民状态变化只发 3 条命令（全量为 102 条）、仅指标变化不触碰任何实体、重排但内容相同的快照零重绘、删除的槽位会被 kill、渲染中途的帧绝不作为基准、发送失败后重试退回全量。全量同步仍保留给首帧 / `anchor` / `demo` / 手动 `refresh`（手动刷新刻意不信任已渲染状态）。JAR SHA-256 `243d4dfe9784fb6bad36136992f3cefe86bdcca9c7cb9e6923f44f1f54b481cc`（两次 `clean build` 一致），已同步 `build/libs`、`minecraft-staging/mods`、`D:\Minecraft\mods` 三处，旧 JAR 备份在 `D:\Minecraft\.codex-backups\scamcity-bridge-0.1.0-pre-incremental-render-20260920-012603.jar`。**验收状态更新（2026-09-20 08:20）**：真机日志首次出现 `增量基准 已建立 | 已发送 450 条 / 跳过 3 帧`，证明 delta 基准与跳帧在游戏内生效（这是之前纯离线验证无法证明的）。但该帧数据源是 `离线演示`、产物是 `243d4dfe...`；**“不闪烁”仍是观感未测量，队列耗时也未测**，live 数据下的增量路径未验。A-12 仍不得宣称完整现场关闭。
- **观众事件模型影响（本轮改为结构化影响，离线已验证）**：`bank-outage`、`deepfake-voice`、`market-panic` 现在各自声明 `affectedStrategies` 并对模型产生有上限的影响（`simulation/audience-events.ts`）：受影响策略的消息会带 `audience-incident` 风险因子，白名单外策略不受影响，并发事件按上限截断，`market-panic` 另有 1.25× 金额乘数。调用方的 `label` 仍只作显示文案。已验证（`node scripts/verify-simulation-contract.mjs`，2026-09-19 生产构建 + 临时端口 3007）：受影响消息 28 条带该因子、白名单外 0 条、同 seed 同事件序列复盘完全一致。**仍未验收**：真实游戏内未观察该因子；也未做敏感度分析，现场应称为 “modeled amplification”，不得说成真实停机/深伪对诈骗率的标定估计。
- **比较口径与主判定（本轮部分关闭）**：`simulation/comparison.ts` 输出 impact / friction / cost 三栏 scorecard，并以单一规则（少损失 → 少受害 → 可接受误报 → 成本可解释）产出带理由的 verdict；`bestComparison()`、API 快照的 `verdict` 字段和 Web 比较面板都用同一规则，不再各自按 `moneyLost` 排序。契约脚本已断言 verdict 的 ranking/理由/三栏字段与 disclaimer。**本轮补上两个缺口**：`scripts/scamcity-report.mjs` 现将 verdict（含 rule/ranking/scorecards/disclaimer）写入 `comparison.json`，已实测输出 `verdict=social-guardian`；Minecraft 端现在通过屏幕叠层显示主判定与 disclaimer。**仍未验收**：叠层上的 verdict 在游戏内未看过。
- **跨 Agent 多进程 soak（已验证）**：2026-09-19 16:12 运行 `node scripts/agent-sync-soak.mjs --agents 16 --seconds 8 --workers 3` → 50/50 checks passed（含 54 次并发续约、旧 lease fencing、幂等重放、receipt 丢失恢复、revision 连续性）。`scripts/agent-sync-smoke.mjs` 同样通过。仍未做的是真实多 pi 长时间（小时级）并发，不属于本项阻塞。证据：`docs/worklogs/2026-09-19-self-iteration.md`。
- **pi 运行时接入（待验收）**：协调层只约定本地文件协议；尚未绑定某个具体 pi 的启动器、Agent ID 生成或 secret store。主办方 token 必须继续留在 pi 自己的运行时环境，不能写入协调状态。
- **协调日志残留（已知）**：`AGENT-STATE/coordination/` 保留历史 `smoke-*` 已完成任务。日志 append-only，删除会破坏 revision 连续性，故不清理；以 `agent-sync status` 磁盘现实为准。smoke 已改为默认隔离运行，不再新增残留。

证据基线：`AGENT-STATE://resources/artifacts.md`；完整重塑状态：`evolution/POST_RESHAPE_STATUS.md`。
