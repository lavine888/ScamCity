# Agent 事件日志

- 2026-09-21 01:45 +08:00 | Codex（/root） | 复用 ScamCity 完成 Web 参赛能力增强：当前快照分叉比较、带激活预算与 stale 保护的干预 Agent、市民故事焦点、叙述决策一致性；TypeScript、专项脚本、HTTP 契约与 Edge 浏览器验收通过。Minecraft JAR 未改变；证据见 `AGENT-STATE://resources/baytech-agent-improvements.md` 与 `docs/worklogs/2026-09-21-agent-improvements.md`。

- 2026-09-21 | root | 用户要求（a）继续改进文档、（b）把 `openai-next` 从 pi 的模型列表里去掉、（c）确认那个 API 是否还有人用。（1）**pi 配置**：从 `~/.pi/agent/models.json` 删除 `providers["openai-next"]`（baseUrl `https://api.openai-next.com/v1`，7 个模型），备份为 `models.json.bak-remove-openai-next-*`；剩余 provider 为 `zhipu`、`tqx`，`settings.json` 的 `defaultProvider` 本来就是 `tqx`，故本次会话（`PI_PROVIDER=tqx`）不受影响。**注意**：`scripts/set-openai-next-key.mjs` 会在下次运行时重新创建该 provider，而项目 `.env.local` 仍持有同一把 key——只删 pi 侧不等于删 key。（2）**文档更正四处**，均为我上一轮 review 指出、用户确认后执行：`PRODUCT_AUDIT.md` 的「PresenterConsole 未找到渲染调用」加就地标注（该组件在 `scamcity-dashboard.tsx:963` 有渲染调用，入口按钮 `:958`）；`ACCEPTANCE_MATRIX.md` A-09 行改标「PARTIAL（见下方更正）」并在表后补一节更正说明；`README.md` 把 LLM 叙述从 Roadmap 移到已实现并补写 `04 / CITIZEN NARRATION` 面板的 live-only 约束；`minecraft-staging/README.md` 把安装路径从 `%APPDATA%\.minecraft\mods` 改为实际 gameDir `D:\Minecraft\mods`，并标注「尚未写入用户实例」一节已过期（该 JAR 早已安装）。**核对时发现一个文档从未记录的新缺陷**：presenter 面板标题按 `PRESENTER_DURATION_MS=180_000` 显示 `3:00`，但阶段间隔 25.5 秒 × 8 阶段 ≈ **204 秒**，标签与真实时长差约 24 秒——已记入 pitfalls.md，**未修代码**（属观感/计时问题，需用户决定是否改）。同时确认 `DEMO MODE`（≈61 秒）与 `3-MIN PRESENTER`（≈204 秒）是两个不同计时器，且两者在 `LIVE API` 下都 disabled。（3）**API 是否还有用**：`api.openai-next.com` 与 pi 已解耦（pi 用 tqx）；但 ScamCity 应用侧仍在使用——`.env.local` 的 `OPENAI_API_KEY`/`OPENAI_BASE_URL`/`OPENAI_MODEL=deepseek-v4-flash` 驱动可选 LLM 适配器，`LLM_ENABLED=true` 时 `/api/llm/thoughts` 与叙述面板依赖它。**本仓不是 Git 仓库**（`git rev-parse` 确认），故 `.env.local` 里的真实 key 没有版本控制兜底，已记入 pitfalls.md 并建议演示后轮换。本轮**只改文档与 pi 配置，未改任何源码、未重新构建**，故 JAR hash 仍为 `a3d289a6...`，现场验收状态未变。

- 2026-09-20 17:05 +08:00 | root | 用户要求补做上一轮挂起的两件事。（1）**接入 ESLint**：`package.json` 早有 `lint: next lint` 脚本，但 eslint 本身从未安装，该命令一直是空指向（非交互环境下会索要创建配置并返回非零，此前被误当作「lint 失败」）。按 `eslint-config-next@14.2.15` 的 peer 范围（`^7.23.0 || ^8.0.0`，**不支持 eslint 9**）以 `--save-exact` 钉死 `eslint@8.57.1` + `eslint-config-next@14.2.15`，新建 `.eslintrc.json`（`next/core-web-vitals`，忽略 `.next/`、`minecraft-bridge/build/`、`evolution/runs/`、`tmp-*/`，显式开 `react-hooks/exhaustive-deps: warn`）。**实测覆盖 26 个文件**（含 `scamcity-dashboard.tsx`、`live-thoughts.ts`、`simulation/world.ts`、API routes），零告警。**做了负向测试**：把 `fetchThoughts` 的 dep array 故意清空，lint 正确报 `missing dependency: dataSource`（即这条规则本可抓住我上一轮自己引入的 ref/state 缺陷），恢复后复检干净、`tsc` 仍 OK。`npm audit` 报 5 项（4 high + 1 critical），已逐条归因：`next`/`postcss` 两条是**本轮之前就存在**的（`next@14.2.15` 自带，未改版本）；本轮唯一引入的是 `glob`，漏洞形态为 **glob CLI 经 `-c/--cmd` 的命令注入**，而 `@next/eslint-plugin-next` 是把 glob 当库调用、不走 CLI，该路径不可达；全部为 devDependencies，不进产物。未执行 `audit fix --force`（会动 `next` 主版本）。（2）**更正过期文档**。先核对发现**我上一轮的建议本身有偏差**：`PRODUCT_AUDIT.md` 第 3 行早已声明自己是「重塑前的发现记录」并指向 `POST_RESHAPE_STATUS.md`，而后者的「已闭环」表确实已记录该能力——所以改写正文结论等于篡改历史记录，不应做。真实问题小得多：grep 跳到第 84 行的人看不到第 3 行的免责声明。故改为**就地状态标注**，正文一字未改。同时发现另外两条 P0（「单一状态源缺失」、「真实运行状态不可判定」）同样已闭环，只标一条会让文档更不一致，故三条一并标注；其中第三条**保留了仍然成立的部分**（`doctor` 的 `minecraft-log`/`command-errors` 读 `latest.log`，新装包后只证明上次加载成功；零命令时 `command-errors` 空过；无副作用权限探针仍未实现）。（3）**顺带修掉三处不实陈述**：`minecraft-bridge/README.md` 把 `16f0d4ad...` 称作「当前现场副本」（落后六版），且首段把 API 地址写成硬编码 `localhost:3000`（端点自发现后不成立），另把构建依赖 `fabric_version=0.141.6` 与**实装** `fabric-api-0.141.4+1.21.11.jar` 的差异写明（签名核对必须针对实装版本；fabric-api 是容器 mod，类在 `META-INF/jars/` 嵌套 JAR 内）；`RUNBOOK.md` 启动路径仍写 `npm run dev`（不产生端点发现文件）、验收段仍引用旧 hash `16f0d4ad...`，已改为 `serve` 并补入端点自发现说明、`ApiEndpoints` 为 `final` 只读一次（serve 若在游戏启动后重启需重进世界）、以及两个易误判点（`/scamcity api` 在首次成功 `refresh` 前必显示「尚未确认」、日志里 `[ZAAT]` 不是本项目），现场清单扩为十一项（新增叠层背板溢出、七态远看可辨性、骗子持书人形与帧数代价、网页叙述面板）；`README.md` 补注演示场景改用 `npm run serve`。**自查修正一处自己造成的破坏**：用 `edit` 改 `acceptance-pending.md` 那条长 bullet 时，`oldText` 只匹配到前半句，替换后该行尾部残留 682 字符旧文，导致同一行既写「live API 已走通」又写「仍未走通」；已按字符偏移截断并复核，同轮修掉第 15 行「`16f0d4ad...` 尚未真机跑过」（其后已有三个版本真机加载）。`edit` 工具本轮又两次把 `edits` 序列化成字符串被参数校验拒绝，重发即好；含大量反斜杠的 `oldText` 匹配不稳，改用 Node 脚本按锚点偏移编辑更可靠。最终 gate：`tsc --noEmit` OK、`next lint` 零告警、`verify-command-format.mjs` passed、`bridge-harness` passed、安装 hash 仍为 `a3d289a6...`（**未重新构建，本轮只改文档与 devDependencies**）。临时文件已清。**现场验收状态未变**：七态配色、骗子人形、叙述面板、叠层四项仍未在真机/浏览器看过。

- 2026-09-20 16:20 +08:00 | root | 用户问「演示版少了点什么，有什么开源工具可以完善」。结论与提问方向相反：**缺的不是工具，是已有资产没接线**（`recharts` 已装从未 import 就是同一症状）。读 `evolution/PRODUCT_AUDIT.md` 后实测两处真缺口并修掉。（1）**LLM 已接通但前端完全没用**：`/api/llm/thoughts` 实测返回 `mode:live, model:deepseek-v4-flash, latency 7.7s`，市民用自己的话解释为什么没上钩且与引擎判定一致（`citizen-030` engine=ignore, p=0.599），但全仓无任何前端调用点，评委看不到。新增 dashboard「04 / CITIZEN NARRATION」面板 + `fetchThoughts`。**仅 live 模式提供**：该路由读服务端 store，local 模式下 dashboard 跑浏览器内自己的世界，叙述会描述另一座城市——本项目把这类错配当正确性缺陷而非观感问题。面板显式标注 `MODEL`/`RULES`、模型名、延迟、`source:fallback` 逐条标记、以及「synthetic citizens, not real people」，因此模型失败降级为确定性思考时不会被误读为模型输出。**自查修正一处自己引入的缺陷**：初版 `fetchThoughts` 用 `thoughtsBusy` 状态做重入保护且把它放进 dep array，但 state 是异步的，两次快速点击可在首次 re-render 前都通过检查；已改为 `thoughtsRequestRef` ref 守卫（与同文件其他 live 回调一致），并移出 dep array 避免回调身份在飞行中变化。（2）**Minecraft 端状态映射残缺，把正在被骗的人画成绿色安全**：`types/index.ts` 的权威词表是 `safe → suspicious → engaged → trusted → clicked → victim` 加 `protected`，而 `status()` 只认 `victim`/`protected`，其余四态全部落到兜底绿色。这一局实测 100 市民中 1 个 `engaged` + 2 个 `trusted` 被误标为安全——而「看见骗局如何展开」正是产品主张，漏斗中段却不可见。已改为七态精确 switch（exact match 优先，避免 `contains("risk")` 这类松匹配吞掉其他状态），控制塔图例同步扩为八项（含骗子紫）。**顺带删掉一段死代码**：旧分支按 `riskScore`/`risk`/`score` 判「高风险」橙色，但市民 payload 从无这些字段（实际是 `riskAwareness`），该分支永不触发、只可能误标。中段三色故意与网页发散：dashboard 把 `engaged`/`trusted`/`clicked` 画成三档橙色，6px SVG 圆点可行，但 100 格网格远看会糊成一片，故拉开为黄/橙/粉；顺序与标签保持一致。`clicked` 初版选 `magenta_concrete`，与骗子的 `purple_concrete` 远看同色，已改 `pink_concrete`。（3）**新增跨语言词表绑定断言**：Java 无法 import TS，且 Java 侧 fixture 只覆盖 `safe`/`victim`，新增状态会静默落到绿色兜底。`verify-command-format.mjs` 现从 `types/index.ts` 解析 `CitizenState` union，逐态要求 Java 有显式分支、块颜色互不重复、不得复用骗子紫、图例必须含全部七个标签。**同轮把一条既有断言从硬编码计数改为不变量**：原「双标签 summon 必须正好 3 处」在我加第四种实体时撞线——计数每加一种实体就要改一次，等于把真检查变成减速带，已改为遍历所有 `summon` 模板逐个校验双标签；另加命令长度断言（按最宽取值渲染后 <250，实测骗子盔甲架最长 154，远低于 256 协议上限）。**两条新断言均做过负向测试**（去掉 slot tag / 删掉 `trusted` 分支都能被抓住，恢复后通过）。**一次工具误判已更正**：首次负向测试报「missing state passed」，实为 `python3` 是 Microsoft Store 存根、脚本从未执行，探针改用 Node 后确认断言真实生效——此前结论是假阴性，不是断言漏洞。全部离线 gate 通过（`tsc --noEmit`、`gradlew --offline check`、`verify-command-format.mjs`、`bridge-harness/verify.mjs`、契约脚本 live）；产物 `a3d289a6...`（两次 `clean build` 一致）已同步三处，旧 JAR 备份为 `...-pre-statecolors-20260920-162030.jar`。**`npx next lint` 在本仓未配置**（会交互式索要创建 ESLint 配置），本轮未代为创建，故 lint 未纳入本轮 gate。**七态配色、图例、叙述面板均未在真机/浏览器现场看过**；`engaged`/`trusted`/`clicked` 三色的远看可辨性属观感，未测量。

- 2026-09-20 11:55 +08:00 | root | 参考 `harrythentrepreneur/omo-minecraft`（AgentCraft）取长补短，落地两项都对应本项目已记录的真实阻塞。（1）**屏幕叠层**：omo 用 BossBar/主手物品/讲台书承载状态，这些通道不占世界实体预算；ScamCity 实体预算已被 100 市民+标签占满，而 A-19 的缺口正是「Minecraft 端不显示主判定」。新增 `HudState.java`（无依赖，进验证源集）+ `ScamCityHud.java`（纯 `fill`/`drawText`，无贴图），经 `HudElementRegistry.addLast` 注册；已用 `javap` 核对 `fabric-api 0.141.6+1.21.11`（本项目现用版本）确实含 hud 包且签名一致。叠层承载 verdict，并强制「DEMO 绝不显示成 LIVE」「verdict 必带 disclaimer」「让位 F3 与任何 screen」，均写成断言。（2）**端口自发现**：新增 `ApiEndpoints.java` + `scripts/scamcity-serve.mjs` + `scripts/lib/endpoint.mjs`。serve 把实际绑定 URL 原子写入 `<tmpdir>/scamcity-endpoint.json`（先写临时再 rename，读方无锁轮询），响应必须含 `schemaVersion` 才被接受——**端口有人应答不等于 ScamCity 在应答**，这正是 08:40 那次误判的信号；显式配置的地址单独返回，避免悄悄连上别的端口掩盖操作者的错误。发现文件在共享临时目录，按不可信输入处理，只接受 loopback `http://`（字面 host 比对，拒绝 `localhost.evil.com`/带凭据/非数字端口）。**已实测复现并修复**：用假监听器占据 3000 模拟 `Open to LAN`，serve 自动改用 57662/51930，`doctor`/`report` 正确找到真实服务（此前 doctor 会在服务健康时报 FAIL——同源缺陷，本轮一并修掉，`scamcity-report.mjs`、`bridge-harness/cli.mjs` 亦同）。顺带把 **verdict 写入 report**（`comparison.json` 新增该字段）。**自查修正一处自己引入的缺陷**：serve 最初用 `spawn("npx", …, {shell:true})`，Node 报 DEP0190（shell 下参数拼接不转义，是命令注入面），改为 `createRequire` 解析 `next/dist/bin/next` 并以 `process.execPath` 直接执行，链路无 shell。新增跨语言常量防漂移断言（Java 无法 import JS，三份 `SCHEMA_VERSION`/`DISCOVERY_FILE` 副本靠契约脚本绑定）。全部离线 gate 通过；产物 `3938312a...`（两次 `clean build` 一致）已同步三处，旧 JAR 已备份。**叠层在游戏内从未看过、HUD 帧数未测、端口自发现的真机路径未验**（证据：`docs/worklogs/2026-09-20-hud-and-endpoint-discovery.md`）。

- 2026-09-20 08:40 +08:00 | root | 市民人形化小试版 + 真机首次连通排查。（1）**真机排查**：用户报「打开单人游戏接不上可视化」，日志反复报 `API 不可用，已切换离线演示: HTTP/1.1 header parser received no bytes`。此文字与 2026-09-19 的 HTTP/2 故障相同但**根因不同**：端口 3000 的监听进程是 Minecraft 自己（PID 21064 = `javaw.exe`，`Open to LAN` 把 LAN 端口分到了 3000），bridge 连上自己的 LAN 服务器。ScamCity 服务实际在 51662。正解是给 profile 加 `-Dscamcity.api=http://localhost:51662/api/simulation`；**不得**把服务改到 3000“对上默认值”。（2）**两个长期阻塞项解除**：日志全程无 `Unknown or incomplete command` 且 status 显示「已发送 450 条」，证明作弊已开、`summon` 成功；同一行的「增量基准 已建立 / 跳过 3 帧」是 **A-12 首次真机证据**——但那帧数据源是 `离线演示`、产物是 `243d4dfe...`，「不闪烁」仍是观感未测量，live 数据下未验。（3）**人形化**（用户选方案 1 并要求先小试）：新增 `/scamcity style blocks|people`，市民可改为染色皮革盔甲架，默认仍为色块（100 个盔甲架帧数代价未测，便宜模式留作退路）。**关键发现**：内联装备 NBT 的 `summon` 实测 561 字符，而命令经 `sendChatCommand` 发出、协议上限 256，超限会被 `add()` 静默丢弃（后果：100 个市民一个也画不出且不报错）；压缩到只留头盒+胸甲仍为 255 超限，故拆为裸 `summon`（155）+ 四条 `item replace`（各约 116），五条落在同一槽位所以不破坏 A-12。`item replace` 必须带 `type=armor_stand`（标签文字共用槽位标签，否则会给 `text_display` 穿衣）。两点已由 `verify-command-format.mjs` 断言锁定（双标签实体计数 2→3、禁止内联装备形式）。（4）**修复 `scamcity-doctor.mjs` 真 bug**：输出过自相矛盾的 `PASS  minecraft-log: profile line not found`——状态由 `loaded` 定、消息由 `version` 定，而启动横幅在日志第 1 行（偏移 24）却只在末 120000 字符窗口里找，日志当时 136763 字节故横幅被切掉、而 `bridge loaded` 标记还在窗口内。已改为全文匹配横幅且状态同时反映两个事实；该 bug 会随日志变长自己冲出。产物 hash 变为 `c04894be...`（两次 `clean build` 一致）已同步三处，旧 JAR 已备份；build/verifyBridgeLifecycle/命令格式/harness/doctor 全通过。**人形模式尚未在游戏内看过**，帧数未测（证据：`docs/worklogs/2026-09-20-humanoid-citizens.md`）。

- 2026-09-20 01:30 +08:00 | root | 关闭验收矩阵 A-12（原 PARTIAL）：新增 `Scene.java`，展示实体改为共享 `scamcity` 标签 + 槽位标签双标签，`SceneQueue` 持有 `Scene` 并以最后一次完整渲染完的帧为 delta 基准。满编 100 格城市单个市民状态变化从 102 条命令降为 3 条；仅指标变化不触碰实体。JAR `243d4dfe...`（两次 `clean build` 一致）已同步三处，旧 JAR 已备份。同轮在 Web 侧关闭 A-04（观众事件结构化影响，带白名单与上限）与 A-19（impact/friction/cost 三栏 + 统一 best 判定）。全部 gate 通过（typecheck、next build、Gradle clean build + verifyBridgeLifecycle、命令格式、契约脚本、bridge-harness、doctor READY、report 全 PASS）。**三项均仅离线验收**，真机未跑；权限阻断未变。证据：`docs/worklogs/2026-09-20-incremental-render.md`。

- 2026-09-19 17:44 +08:00 | root | **首次真机加载**：17:16:56 日志出现 `ScamCity bridge loaded`，模组在 Fabric 1.21.11 / Loader 0.19.3 下可正常加载（这是之前所有离线验收都未能证明的事）。现场暴露两个问题：（1）**代码 bug（已修）**——`HttpClient` 未指定版本，JDK 默认 HTTP/2 对明文 `http://` 尝试 h2c 升级，Next.js dev server 直接断开，报 `IOException: HTTP/1.1 header parser received no bytes` 且请求不达服务端日志；已固定为 `HTTP_1_1`，Java 探针连跑 5 次零偏差（HTTP_2 必败 / HTTP_1_1 必成 200，130191 字节），代理经 `ProxySelector=[DIRECT]` 排除（最初怀疑 clash 是错的方向）。（2）**环境（未解决）**——存档 `allowCommands=0`，`summon`/`kill`/`title` 全部回显 `Unknown or incomplete command`，需用户 GUI 开 `Allow Cheats`。产物 hash 变为 `16f0d4ad...`（两次 clean build 一致）已安装，旧 JAR 已备份；readiness gate 全通过。修复后的 JAR **尚未在游戏内跑过**（证据：`docs/worklogs/2026-09-19-http1-fix-and-live-attempt.md`）。
- 2026-09-19 16:54 +08:00 | root | 关闭验收矩阵 A-13（原 FAIL）的主体：新增 `CitizenGrid.java`，市民网格从数组下标改为按 `citizen.id` 稳定投影（FNV-1a + 排序去重 + 线性探测，按 cell 序发命令以保持同画面去重）。新增 10 项回归均 PASS；对真实 live API 的 100 个 id 复算为 100 个不同 cell。产物 hash 变为 `7adfb509...`（两次 clean build 一致）并已装入 staging 与 `D:\Minecraft\mods`，旧 JAR 已备份；readiness gate 全通过。`layout.json` 外部布局未实现，现场不跳位待验收（证据：`docs/worklogs/2026-09-19-stable-citizen-grid.md`）。
- 2026-09-19 16:12 +08:00 | root | 自我迭代：跑通长期挂起的多进程 soak（16 agents × 3 workers × 8s，50/50 checks passed）；发现并修复 `agent-sync-smoke.mjs` 会污染共享协调日志的缺陷（改为默认临时隔离根，`--shared` 才写共享，并新增终态断言，验证后共享 revision 保持不变）；完整 readiness gate 通过（doctor API/JAR PASS、契约 OK、report 全 PASS、bridge-harness passed），确认 `D:\Minecraft\mods` 已加载新 hash。真实游戏内 E2E 仍待验收（证据：`docs/worklogs/2026-09-19-self-iteration.md`）。
- 2026-09-19 15:57 +08:00 | root | 接手刷新稳定性迭代并完成离线验收：`verify-command-format` 通过、`gradlew check` 的 `verifyBridgeLifecycle` PASS、`gradlew clean build` 成功且 hash 可复现；新 JAR `a223c206...` 已同步到 `minecraft-staging/mods` 与 `D:\Minecraft\mods`（旧 JAR 已备份）。真实游戏内 E2E 仍待验收（证据：`docs/worklogs/2026-09-19-bridge-refresh-verification.md`）。
- 2026-09-19 15:57 +08:00 | root | 更正：早先条目称 smoke “测试事件/receipt/lease 已清理”，但磁盘上 `AGENT-STATE/coordination/` 仍保留 2 个已完成的 smoke 任务与对应事件/receipt（revision 13 时 eventCount 13）。后续 Agent 不得假定共享队列为空；以 `status` 磁盘现实为准。
- 2026-09-19 15:44 +08:00 | root | 同步进行中的 Minecraft 稳定性迭代：记录文件所有权、SceneQueue/RequestEpoch 源码状态、队列边界问题及构建/现场待验收；staging hash 仍为旧基线，详见 AGENT-STATE://memories/current-iteration.md。
- 2026-09-19 | root | 产品改进只读复核：BridgeController.java 的 applySnapshot 仍清空并重建 block/text displays，enqueueCitizens 使用固定网格；agents/live-thoughts.ts 为叙述层、不覆盖引擎决策。建议优先真实 Minecraft 验收、角色空间呈现、增量刷新、事件行为闭环；本轮未修改产品代码、未执行游戏验收。
- 2026-09-19 | root | 为协调层首次并发启动补上 bootstrap manifest 锁，最终 smoke 覆盖初始化、交接和完成路径并通过；测试事件/receipt/lease 已清理（证据：`docs/worklogs/2026-09-19-agent-sync.md`）。
- 2026-09-19 | root | 补强 `reap` 的 crash recovery：恢复已有 `task.lease-expired` 事件时按 leaseId/generation fencing 清理旧 lease，最终 smoke 再次通过，运行产物已清理（证据：`docs/worklogs/2026-09-19-agent-sync.md`）。
- 2026-09-19 | root | 补验 `scripts/agent-sync-smoke.mjs`：首次受限执行受 `spawn EPERM` 阻断，修正 init JSON 输出后在沙箱外完整 smoke 通过，测试状态已清理；长时间多 pi soak 仍待验收（证据：`docs/worklogs/2026-09-19-agent-sync.md`）。
- 2026-09-19 | root | 新增 `scripts/agent-sync.mjs` 本地跨 Agent 协调协议与 `AGENT-STATE://coordination/` 文档；通过 `npm run typecheck`、Node 语法检查及手工 enqueue/claim/heartbeat/handoff/complete、旧 lease 拒绝、过期回收、幂等恢复和凭据拒绝验收，内置 smoke 的子进程执行受当前沙箱 `spawn EPERM` 限制（证据：`docs/worklogs/2026-09-19-agent-sync.md`）。
- 2026-09-19 | root | 首次接入跨 Agent 状态库：创建 OpenViking 分层骨架，记录当前 bridge 产物、验证边界和待验收 Minecraft E2E。
